import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    title: {
        flexGrow: 1,
        display: 'flex',
        textDecoration: 'none',
        cursor: 'pointer',
        alignItems: 'center'
    },
    image: {
        marginRight: '10px',
    }

}))